<UserControl x:Class="Bosco.XAML.Controls.Filters.MainFilterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Bosco.XAML.Controls.Filters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <StackPanel Margin="15">
            <TextBlock Text="FILTRAR POR"/>
            <Separator Height="1"/>
        </StackPanel>
        
        <ContentPresenter Content="{Binding Path=FilterContent, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type local:MainFilterView}}}"
                          Grid.Row="1"
                          Margin="15 20 15 0"/>

        <Button Content="FILTRAR"
                HorizontalAlignment="Right"
                Margin="15"
                Command="{Binding Path=FilterCommand, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type local:MainFilterView}}}"
                Grid.Row="2"/>
    </Grid>
</UserControl>
