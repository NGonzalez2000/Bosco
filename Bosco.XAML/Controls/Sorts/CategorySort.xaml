<UserControl x:Class="Bosco.XAML.Controls.Sorts.CategorySort"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:sortmodels="clr-namespace:Bosco.Core.Models.SortModels;assembly=Bosco.Core"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:Bosco.XAML.Controls.Sorts"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type={x:Type sortmodels:CategorySortModel}}"
             d:DesignHeight="450" d:DesignWidth="800">
    <UniformGrid Columns="2">
        <ComboBox ItemsSource="{Binding Path=SortOptions}"
                  SelectedItem="{Binding Path=SelectedOption}"
                  IsEnabled="False"
                  Style="{StaticResource MaterialDesignFloatingHintComboBox}"
                  materialDesign:HintAssist.Hint="Criterio"
                  Margin="15 0"/>
        <ComboBox ItemsSource="{Binding Path=SortOrders}"
                  SelectedItem="{Binding Path=SelectedOrder}"
                  Style="{StaticResource MaterialDesignFloatingHintComboBox}"
                  materialDesign:HintAssist.Hint="Orden"
                  Margin="15 0"/>
    </UniformGrid>
</UserControl>
